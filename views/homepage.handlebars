    <div class="mb-5 pt-3">
        
        <h2 class="title pb-4">Blogs</h2><hr>
{{#if posts.length}}
    

    {{#each posts as |post|}}
        {{> post-details post}}
        
                  
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">There is no posts</h6>

{{/if}}
</div>